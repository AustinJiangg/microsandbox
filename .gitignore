__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.venv/
venv/
.env
*.log
.DS_Store

# Claude Code 的本地个人配置（权限白名单等），因人而异不入库
.claude/settings.local.json

# 阶段 3：Firecracker 素材（二进制 / 内核 / rootfs 镜像），体积大、可重新生成，不入库。
# 来源与构建步骤见 docs/STAGE3_DESIGN.md 与 scripts/。
vendor/
