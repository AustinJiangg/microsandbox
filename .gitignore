__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.venv/
venv/
.env
*.log
.DS_Store

# Claude Code's local personal config (permission allowlist, etc.); per-user, not committed
.claude/settings.local.json

# Stage 3: Firecracker artifacts (binary / kernel / rootfs image) -- large and
# regenerable, so not committed. See docs/STAGE3_DESIGN.md and scripts/ for sources and build steps.
vendor/
